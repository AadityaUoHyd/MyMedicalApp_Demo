#Spring DataSource (DataSourceAutoConfiguration & DataSource Properties) - Create one db named 'boot' inside MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/boot?useSSL=false
spring.datasource.username=root
spring.datasource.password=root

#The Hibernate Properties
#The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

#Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE

#In case, Default username/password for Spring security one want to set.
#spring.security.user.name=aadi
#spring.security.user.password=passwd
#spring.security.user.roles=USER